version: '3.3'
services:
  ffmpeg:
    build:
      context: .
    container_name: ffmpeg
    volumes:
      - ./rot_90:/input
      - ./rot_90_done:/output
    entrypoint: ["bash", "/home/run_batch.sh"]
